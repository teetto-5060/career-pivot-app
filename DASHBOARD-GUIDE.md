# 📊 데이터 분석 대시보드 추가

## ✅ 새로 추가된 기능

### 📍 대시보드 페이지
```
URL: http://localhost:3000/dashboard
```

### 📊 주요 지표

**1. 전체 통계 카드**
- 총 제출 건수
- 고유 사용자 수
- 평균 재방문율
- 오늘 제출 건수

**2. 사업 유형별 통계**
- 각 사업 유형별 제출 건수
- 백분율 표시
- 진행 바 차트

**3. 일별 제출 추이**
- 최근 7일간 제출 추이
- 일별 막대 그래프

**4. 이메일 도메인 TOP 10**
- 가장 많이 사용된 이메일 도메인
- 도메인별 사용자 수

**5. 시간대별 제출 현황**
- 24시간 기준 제출 패턴
- 피크 타임 분석

**6. 최근 제출 목록**
- 최근 20건의 제출 상세 정보
- ID, 이름, 이메일, 사업 유형, 아이템, 제출 시간

---

## 🚀 사용 방법

### 1. 파일 추가

새로 추가된 파일들:
```
app/
├── api/
│   └── dashboard/
│       └── route.ts          ← 대시보드 API
└── dashboard/
    └── page.tsx              ← 대시보드 UI
```

### 2. 로컬에서 확인

```bash
# 개발 서버 실행 (이미 실행 중이면 새로고침만)
npm run dev
```

브라우저에서:
```
http://localhost:3000/dashboard
```

### 3. 실시간 업데이트

**"데이터 새로고침" 버튼** 클릭하면 최신 데이터 로드!

---

## 📊 대시보드 스크린샷 예시

```
┌─────────────────────────────────────────────────────────┐
│  📊 Teetto 대시보드                                     │
│  시니어 창업 지원 플랫폼 분석                             │
└─────────────────────────────────────────────────────────┘

┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│총 제출    │  │고유 사용자│  │재방문율   │  │오늘 제출  │
│   15     │  │    12    │  │   25%    │  │    3     │
└──────────┘  └──────────┘  └──────────┘  └──────────┘

┌─────────────────────────┐  ┌─────────────────────────┐
│ 사업 유형별 통계          │  │ 최근 7일 제출 추이       │
│                         │  │                         │
│ 교육형 ████████ 40%     │  │ 01/20 ████████ 3건      │
│ 상담형 ██████ 30%       │  │ 01/19 ████ 1건          │
│ 기술형 ████ 20%         │  │ 01/18 ██████ 2건        │
│ 중개형 ██ 10%           │  │ ...                     │
└─────────────────────────┘  └─────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 최근 제출 목록                                           │
├────┬──────┬─────────────────┬───────────┬────────────┤
│ ID │ 이름 │ 이메일          │ 사업 유형 │ 제출 시간   │
├────┼──────┼─────────────────┼───────────┼────────────┤
│ #3 │홍길동│hong@example.com │ 교육형    │ 01/20 14:30│
│ #2 │이영희│lee@example.com  │ 상담형    │ 01/20 12:15│
│ #1 │김철수│kim@example.com  │ 기술형    │ 01/19 16:45│
└────┴──────┴─────────────────┴───────────┴────────────┘
```

---

## 💡 활용 방법

### 1. 마케팅 분석
```
✅ 어느 시간대에 사용자가 많은가?
✅ 어떤 사업 유형이 인기 있는가?
✅ 재방문 사용자는 몇 %인가?
```

### 2. 타겟 분석
```
✅ 주로 어떤 이메일 도메인을 사용하는가?
   → Gmail 많으면 개인, Naver 많으면 한국인
✅ 어떤 직군이 많이 사용하는가?
```

### 3. 비즈니스 인사이트
```
✅ 교육형이 40%? → 교육 컨텐츠 강화
✅ 오후 2-4시에 피크? → 그 시간에 이메일 발송
✅ 재방문율 낮음? → 리마케팅 강화
```

---

## 🔒 보안 (중요!)

**현재 대시보드는 누구나 접근 가능합니다!**

실제 배포 시 **인증 추가** 필요:

### 옵션 1: 간단한 비밀번호

```typescript
// app/dashboard/page.tsx 맨 위에 추가
'use client';
import { useState, useEffect } from 'react';

export default function Dashboard() {
  const [authenticated, setAuthenticated] = useState(false);
  const [password, setPassword] = useState('');
  
  const handleLogin = () => {
    if (password === 'teetto2026') {  // 비밀번호 변경!
      setAuthenticated(true);
      localStorage.setItem('dashboardAuth', 'true');
    } else {
      alert('비밀번호가 틀렸습니다.');
    }
  };
  
  useEffect(() => {
    if (localStorage.getItem('dashboardAuth') === 'true') {
      setAuthenticated(true);
    }
  }, []);
  
  if (!authenticated) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-gray-100">
        <div className="bg-white p-8 rounded-lg shadow-lg">
          <h2 className="text-2xl font-bold mb-4">대시보드 접근</h2>
          <input
            type="password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            onKeyPress={(e) => e.key === 'Enter' && handleLogin()}
            className="w-full px-4 py-2 border rounded mb-4"
            placeholder="비밀번호 입력"
          />
          <button
            onClick={handleLogin}
            className="w-full bg-primary text-white py-2 rounded"
          >
            로그인
          </button>
        </div>
      </div>
    );
  }
  
  // 기존 대시보드 코드...
}
```

### 옵션 2: Vercel 인증

```
Vercel 배포 시:
Settings → Environment Variables
→ DASHBOARD_PASSWORD=your-secret-password
```

---

## 📈 고급 기능 (추후 추가 가능)

### 1. CSV 내보내기
```typescript
const exportCSV = () => {
  const csv = data.recent.map(r => 
    `${r.id},${r.name},${r.email},${r.selected_business_type}`
  ).join('\n');
  
  const blob = new Blob([csv], { type: 'text/csv' });
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'submissions.csv';
  a.click();
};
```

### 2. 필터링
```typescript
const [dateFilter, setDateFilter] = useState('7days');
// 7일, 30일, 전체 선택

const [typeFilter, setTypeFilter] = useState('all');
// 사업 유형별 필터
```

### 3. 실시간 업데이트
```typescript
useEffect(() => {
  const interval = setInterval(fetchDashboardData, 30000); // 30초마다
  return () => clearInterval(interval);
}, []);
```

---

## 🎯 다음 단계

### 1. 로컬에서 테스트
```bash
npm run dev
# http://localhost:3000/dashboard 접속
```

### 2. 메인 페이지에 링크 추가 (선택)

`app/page.tsx`에 대시보드 링크:
```tsx
<a href="/dashboard" className="text-sm text-gray-500 hover:text-primary">
  📊 관리자 대시보드
</a>
```

### 3. Vercel 배포
- GitHub 푸시
- Vercel 자동 배포
- https://your-app.vercel.app/dashboard

---

## 🎉 완성!

이제 데이터를 실시간으로 분석할 수 있습니다!

**대시보드 URL:**
- 로컬: http://localhost:3000/dashboard
- 배포 후: https://your-domain.com/dashboard

**보안 설정 잊지 마세요!** 🔒
